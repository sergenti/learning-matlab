mean(A(:))
mean(A, "all")
e uguale a
mean mean a

mean a colonne somma 1 sola riga
mean a, 1 colonne

1 sola colonna
mean a 2 righe

sum diff mean prod

sum A somma per righe, su c

[i, j] = find (rem(a, 3) == 0)

muoveri sulle righe

for i = 1:size(a, 1)
    whose

    edit funzione

    format

    format long
    pi

    formax HEX
    format bank

    SHORTE LONGE

    A = input("inserisci wadhlja")

    studia plot

    axis
    subplot

    3 * ones(4, 5)

    A(2, :) = 1:5
    sosttuisce la riga 2

    A(:; 1:2:end) = 10
    A(:; 1:2:end) = 10 * ones(size(A:, 1, 2:end))

    numberi dispari con zero
    A(rem(a, 2) != 0) = 0

    rem(A, 2) != 0 rida matice con zero e uno
    mettendo sta roba dentro

    ardice somma di a
    sqrt(sum(sum(A)))
    sqrt(sum(A, "all"))
    sqrt(sum(A(:)))

    soluzione più generale possibile

    A(:, mean(A) > 0) = []
    mean M(M(M > 3))

    sommatoria da 1 a n

    rem a n 00 0
    ù
